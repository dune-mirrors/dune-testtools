# Following lines will be filtered as one ini file
a = 2, 3 | expand
{a} == 2 | exclude

# Following lines will be filtered as one ini file
b = 2, 3 | expand
{b} > 2 | exclude

# Following lines will be filtered as one ini file
c = 2, 3 | expand
2 < {c} | exclude

# Following lines will be filtered as one ini file
d = 2, 3 | expand
"2 == {d}" | exclude

# Following lines will be filtered as one ini file
e = 2.55, 12.55 | expand
{e} < 10 | exclude

